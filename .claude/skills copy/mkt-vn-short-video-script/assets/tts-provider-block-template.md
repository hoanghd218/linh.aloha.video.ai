<!--
TTS provider metadata footer.

Append this YAML-fenced block at the BOTTOM of every `brief.md` generated by
`/mkt-vn-short-video-script`. Pipeline orchestrator downstream parses this
block to know which TTS skill to call (Phase 1) and which aesthetic override
to apply (Phase 3), without re-asking the user.

Replace placeholders before writing:
- tts_provider: one of `elevenlabs` | `minimax`
- profile: the profile name used (must match a folder under `profiles/`)
-->

---
tts_provider: <elevenlabs|minimax>
profile: <profile-name>
---
